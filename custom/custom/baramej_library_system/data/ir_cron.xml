<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Cron Job: Update Overdue Loans -->
    <record id="ir_cron_update_overdue_loans" model="ir.cron">
        <field name="name">Library: Update Overdue Loans</field>
        <field name="model_id" ref="model_library_borrow"/>
        <field name="state">code</field>
        <field name="code">model._cron_update_overdue_status()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="active" eval="True"/>
        <field name="doall" eval="False"/>
        <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=3, minute=0, second=0)"/>
    </record>

    <!-- Cron Job: Send Due Soon Reminders -->
    <record id="ir_cron_send_due_reminders" model="ir.cron">
        <field name="name">Library: Send Due Soon Reminders</field>
        <field name="model_id" ref="model_library_borrow"/>
        <field name="state">code</field>
        <field name="code">model._cron_send_due_reminders()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="active" eval="True"/>
        <field name="doall" eval="False"/>
        <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=9, minute=0, second=0)"/>
    </record>

    <!-- Cron Job: Send Overdue Reminders -->
    <record id="ir_cron_send_overdue_reminders" model="ir.cron">
        <field name="name">Library: Send Overdue Reminders</field>
        <field name="model_id" ref="model_library_borrow"/>
        <field name="state">code</field>
        <field name="code">model._cron_send_overdue_reminders()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="active" eval="True"/>
        <field name="doall" eval="False"/>
        <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=10, minute=0, second=0)"/>
    </record>
</odoo>
